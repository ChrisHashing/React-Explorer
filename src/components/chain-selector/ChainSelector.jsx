import React from 'react';
import styles from './chain-selector.module.css';

const ChainSelector = () => {
  return (
    <div className={styles.container}>
      <h2 className={styles.title}>Select a Blockchain</h2>
      <p>This is a placeholder for the Chain Selector component.</p>
    </div>
  );
};

export default ChainSelector;
